# Admin Panel Environment Configuration
# Copy this file to .env.local and configure as needed

# API Configuration (TASK-P2-ADM-01)
# For local development with Vite proxy:
VITE_API_BASE_URL=/api/v1

# For production or when admin is hosted separately from backend:
# VITE_API_BASE_URL=https://api.example.com/api/v1

# Resilience Configuration (TASK-P2-ADM-02)
# Request timeout in milliseconds
VITE_API_TIMEOUT=15000

# Maximum retry attempts for GET requests
VITE_API_MAX_RETRIES=3

# Base retry delay in milliseconds (uses exponential backoff)
VITE_API_RETRY_DELAY=1000

# App Configuration
VITE_APP_TITLE=AntyGravity Admin

# =============================================================================
# Production Notes
# =============================================================================
# For production deployment:
# 1. Set VITE_API_BASE_URL to your actual API domain
# 2. Build with: npm run build
# 3. Serve the dist/ folder via nginx/CDN with proper CSP headers
