version: "3.8"

# =============================================================================
# DTG Docker Compose - Infrastructure Services ONLY
# =============================================================================
# Backend and Admin run locally from repo, NOT in Docker.
# This file provides: PostgreSQL, Redis, Biometric-service
# =============================================================================

services:
  postgres:
    image: postgres:15-alpine
    container_name: dtg-postgres
    environment:
      POSTGRES_DB: dtg
      POSTGRES_USER: dtg_user
      POSTGRES_PASSWORD: dtg_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/db/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dtg_user -d dtg"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  redis:
    image: redis:7-alpine
    container_name: dtg-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  biometric-service:
    build: ./biometric-service
    container_name: dtg-biometric-service
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: always

  dtg-shield-service:
    build: ./dtg-shield-service
    container_name: dtg-shield-service
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
      - REDIS_URL=redis://redis:6379/0
      - BACKEND_URL=http://host.docker.internal:3000
      - BLOCK_THRESHOLD=100
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: always

volumes:
  postgres_data:
  redis_data:
